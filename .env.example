# --- Database ---
POSTGRES_USER=redline
POSTGRES_PASSWORD=redline_dev_password
POSTGRES_DB=redline
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
DATABASE_URL=postgresql+psycopg2://redline:redline_dev_password@localhost:5432/redline

# --- Auth ---
JWT_SECRET_KEY=change_this_to_a_long_random_string
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# --- LLM provider (used by the RAG agent + the llm_judge scorer) ---
ANTHROPIC_API_KEY=

# --- GitHub Checks integration (added in Section 12, not needed yet) ---
GITHUB_TOKEN=